import React from 'react'
import { MyPage } from "@/models/common";

type Props = {}

const MovieFormat:MyPage = (props: Props) => {
  return (
    <div>MovieFormat</div>
  )
}

export default MovieFormat;
MovieFormat.Layout = "Admin";